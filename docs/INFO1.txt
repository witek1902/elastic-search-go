Cel przedmiotu:
Potrzebna podstawowa wiedza z dziedziny baz danych. Bêd¹ j¹ przypomina³ podczas wyk³adu. W ramach przedmiotu poka¿ê rózne metody tworzenia aplikacji maj¹cych u swojej podstawy baze danych. 
Od standardowych klient - serwer) po trójwarstwowe (dla uzytkownika wystarcza w nich przegladarka internetowa). 
Bêdê g³owny nacisk k³adl na obiektowe podejscie do budowy aplikacji. 
Na zaliczenie kazdy student powinien stworzyæ "mikro" aplikacjê wspó³pracuj¹c¹ z baz¹ danych (w dowolnej technice i dowolnym narzêdziu jakie student wybierze).

Treœci merytoryczne:
Wyk³ad
1. Przygotowanie bazy danych do wspó³pracy z wybranym rodzajem aplikacji klienckiej
2. Projektowanie baz danych (co siê przydaje w praktyce)
3. Architektura Klient-Server
4. Architektura trójwarstwowa. Laboratorium
5. Zaprojektowanie i wykonanie prostej bazy
6. Zaprojektowanie i wykonanie interfejsu do bazy w technologi ASPX .NET (C#)
- narzêdzia wizualne oparte o SQLDataSource i generatory kodu
- programowe tworzenie interfejsu
- separacja
    - mapowanie relacyjnych baz danych do obiektów
    - operacje na obiektach
- JQuery
- tworzenie warstwy odpowiedzialnej za dostêp do danych
Literatura przedmiotu:
Udostêpniam studentom mój skrypt w postaci prezentacji i cwieczen w SQL-u. Dostarczam program zakladajacy baze danych (testow¹) i przyk³ady,'
 które na³e¿y wykonaæ i opis jak nale¿y je wykonaæ
Metody oceny:
wykonanie prostej bazy i interfejsu do niej. Generalnie wykonanie projektu


Witam,
Bardzo póŸno zaczynamy i macie Pañstwo coraz wiêcej zaliczeñ maj/czerwiec

Dlatego gor¹ca proœba:

1. Poczytaæ za³¹czone 2 PDF-y
2. Pobawiæ siê programami
3. Napisaæ maluteñki projekcik

Interface do jednej wybranej encji (albo miasta, albo firmy, albo osoby albo etaty)

- gridview z opcj¹ szukania (wystarczy po jednym najbardziej znacz¹cym polu) pokazuj¹cy listê znalezionych danych
- gridview zawiera Select/Edit/Delete -> proœba o oprogramowanie
- detailview do edycji/wprowadzenia nowych danych

Wyznaczam termin do 7 czerwca do pó³nocy. 8 czewca wpisy od 10.30 w sali 510 GE
Tydzieñ spóŸnienia nie obni¿a oceny ale apeluje o wczeœniejsze oddanie
Drugi termin oddania i wpisów to 14 czerwca do 23.59 i wpisy 15 czerwca o 10.30 sala 510 GE

W razie jak coœ bêdzie niezrozumia³e to proszê zebraæ grupê chêtnych i zrobimy wyjaœniaj¹ce zaj¹ecia

Smia³o prosze pisac na maciej.stodolski@gmail.com (politechniczny adres czesto nie dziala)

Pozdrawiam

Maciej

1. Metody tworzenia aplikacji bazodanowych
Wybrana encja: Osoby
W bazie danych ustawi³em, by ID miasta mog³o byæ NULL (ze wzglêdu na to, ¿e mieliœmy zrobiæ interfejs dla jednej encji).
Dziêki temu nie musia³em bawiæ siê w pobieranie listy miast i wpisywanie ich przy tworzeniu nowego u¿ytkownika.

Wykonane:
- wyszukiwanie po nazwisku w tym samym GridView, co wyœwietlanie ca³ej listy; zmieniony zostaje tylko bindowany SQLDataSource
- GridView zawiera Edycje, Usuwanie oraz Wybieranie ka¿dej kolumny
- Edycja odbywa siê bezpoœrednio w tabeli poprzez UpdateCommand SQLDataObject
- Usuwanie odbywa siê bezpoœrednio w tabeli poprzez DeleteCommand SQLDataObject
- Wybranie wiersza spowoduje wyœwietlenie siê DetailView w trybie ReadOnly
- Wprowadzanie nowej osoby odbywa siê w nowym Panelu, po wprowadzeniu przenosi do ogólnego widoku


